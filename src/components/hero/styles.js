import styled from 'styled-components';

const StyledHero = styled.div`
	padding: 1rem;
`;

const MainTitle = styled.h1`
	font-size: 3.75rem;
	text-align: center;
`;

const ItalicText = styled.span`
	font-style: italic;
`;

export { StyledHero, MainTitle, ItalicText };
